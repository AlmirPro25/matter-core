# Evolução do sistema — 2026-07-14

## Feito nesta sessão

### Build / Windows
- Target local no **D:** (`target/x86_64-pc-windows-gnu/`), sem `F:\matter_target`
- MinGW-w64 em `D:\mingw64\mingw64`
- `matter-cli` **release** ~46 MB, smoke e contratos OK

### Testes
- `cargo test` nos crates core (lexer, parser, ast, bytecode, compiler, vm, backend, stdlib, kernel-vm, error, optimizer): **11/11 pass**
- Post-release smoke: **14/14 pass**
- Status triad (core/world/frontier) OK

### Distribuição
- Package: `dist/matter-core-windows-x64/` + `.zip` (~15 MB)
- Verify package: **ok**
- Install local slim: **`D:\Matter`** (PATH do usuário apontando para `D:\Matter\bin`)

### Scripts evoluídos
- `scripts/build-matter-cli.ps1`
- `scripts/install-local.ps1` v0.1.7 (slim, prefere D:, evita encher C:)
- `scripts/build-release-package.ps1` (candidatos GNU)

## Como usar agora

```powershell
# instalado
D:\Matter\bin\matter.exe --help
D:\Matter\bin\matter.exe core-status-json
D:\Matter\bin\matter.exe run D:\Matter\examples\hello.matter

# ou no repo
cd "D:\Users\almir\Desktop\MANIFESTO DA LINGUAGEM MATTER CORE"
.\target\x86_64-pc-windows-gnu\release\matter-cli.exe run examples\hello.matter
```

## Atenção crítica de máquina
- **Disco C: ~0.07 GB livre** — quase cheio. Não instalar em `%LOCALAPPDATA%` até liberar espaço.
- Usar **D:** para Matter (`D:\Matter`, projeto, target, dist).

## Ainda experimental
- `production_ready: false`
- Frontiers simulados / sem hardware
- FFI com blockers de amplitude (Node/Go/Java/Rust arbitrary)

## Próximos passos de evolução
1. Liberar espaço no C:
2. `cargo test` em crates de bridges + runtime (além do core)
3. LSP smoke no editor
4. Beta site / setup exe se necessário
5. Apps reais em `.matter` e feedback de linguagem
