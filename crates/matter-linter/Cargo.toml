[package]
name = "matter-linter"
version = "0.7.0"
edition = "2021"

[dependencies]
matter-ast = { path = "../matter-ast" }
matter-parser = { path = "../matter-parser" }
serde = { version = "1.0", features = ["derive"] }
