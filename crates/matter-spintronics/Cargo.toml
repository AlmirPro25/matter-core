[package]
name = "matter-spintronics"
version = "0.1.0"
edition = "2021"
authors = ["Matter Core Team"]
description = "Spintronics computing support for Matter - spin-based computation"
license = "MIT"
repository = "https://github.com/matter-lang/matter"

[dependencies]
matter-backend = { path = "../matter-backend" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
