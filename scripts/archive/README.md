# Archived Scripts

These scripts are kept only for historical context.

They include earlier workspace organization and move helpers with hardcoded local paths. Do not use them for normal development.

Use active scripts from `scripts/` instead:

```powershell
.\scripts\preflight-env.ps1
.\scripts\validate-full-workspace.ps1
.\scripts\test_all.ps1
```
