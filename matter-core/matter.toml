[package]
name = "matter-core"
version = "0.1.0"
entry = "examples/live_counter_app.matter"

[paths]
stdlib = "stdlib"
store = ".matter_store.json"

[dependencies]
math_tools = "examples/modules/math_tools.matter"
